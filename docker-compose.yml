version: "3.8"

services:
  protoncalbridge:
    build: .
    container_name: protoncalbridge
    network_mode: host
    volumes:
      - ./data:/data
    env_file:
      - .env
    restart: unless-stopped
