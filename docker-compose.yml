version: "3.8"

services:
  protoncalbridge:
    build: .
    container_name: protoncalbridge
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    env_file:
      - .env
    restart: unless-stopped
